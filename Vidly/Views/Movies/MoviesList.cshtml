@*@model Vidly.ViewModels.MoviesListViewModel
@{
    ViewBag.Title = "MoviesList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<table class="table">
    <thead class="thead-dark">
        <tr>
            <th scope="col">Movies List</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var movie in Model.MoviesList)
        {
            <tr>
                <td>@Html.ActionLink(movie.Name, "ShowMovie", "Movies", new { Id = movie.Id }, null)</td>
            </tr>

        }
    </tbody>
</table>*@